import styled from '@emotion/styled';
import { Button } from '@mui/material';

export const ButtonSubmitOutlined = styled(Button)`
  background-color: #ffffff;
  color: #0bafdd;
  font-size: 12px;
  font-weight: bold;
  border-radius: 50px;
  padding: 5px 15px;
  box-shadow: inset 0 0 0 2px #0bafdd;
`;
